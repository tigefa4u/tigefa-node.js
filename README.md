tigefa-node.js `gh-pages`
========================

this branch for documentation is using @jekyll

### `bundle` gems

```ssh
bundle install
```

### `bower`_components

```ssh
bower install
```

### now it `jekyll` serve

```ssh
jekyll serve -w
```